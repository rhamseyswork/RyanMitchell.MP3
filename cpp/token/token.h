#ifndef TOKEN_H
#define TOKEN_H

#include <string>

class GenerateToken {
public:
    GenerateToken(); // Constructor declaration

    std::string generateToken(int length); // Method declaration
};

#endif // TOKEN_H
